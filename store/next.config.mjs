/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ["i.pinimg.com", "encrypted-tbn0.gstatic.com", "themes.pixelstrap.com"]
    }
};

export default nextConfig;
