{
  "buildCommand": "cd ../.. && yarn install && yarn turbo run build --filter=admin...",
  "outputDirectory": "dist",
  "installCommand": "yarn install",
  "framework": "vite",
  "rewrites": [{ "source": "/(.*)", "destination": "/index.html" }]
}
